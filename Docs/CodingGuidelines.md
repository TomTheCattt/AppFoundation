# Coding Guidelines

- Prefer protocols over inheritance
- Use dependency injection; avoid singletons except for managers
- Use async/await for concurrency
- Avoid force unwrap in production code
- Follow SwiftLint and SwiftFormat config in Scripts/
